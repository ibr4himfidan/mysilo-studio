'use client';
import {useEffect,useRef,useState} from 'react';
import * as THREE from 'three';
import {OrbitControls} from 'three/addons/controls/OrbitControls.js';
import {createAsset} from '@/lib/asset-model';
import {disposeModel} from '@/lib/models';
export default function AssetViewer({id}:{id:string}){const ref=useRef<HTMLDivElement>(null),[error,setError]=useState(false);useEffect(()=>{if(!ref.current)return;const host=ref.current;let renderer:THREE.WebGLRenderer;try{renderer=new THREE.WebGLRenderer({antialias:true})}catch{setError(true);return}renderer.setClearColor(0xedf2f4);renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));host.appendChild(renderer.domElement);const scene=new THREE.Scene();scene.add(new THREE.HemisphereLight(0xffffff,0x809198,3));const light=new THREE.DirectionalLight(0xffeed9,4);light.position.set(-15,40,30);scene.add(light);const model=createAsset(id);scene.add(model);const bounds=new THREE.Box3().setFromObject(model),center=bounds.getCenter(new THREE.Vector3()),size=bounds.getSize(new THREE.Vector3()).length();const camera=new THREE.PerspectiveCamera(35,1,.1,500);camera.position.set(center.x+size*.85,center.y+size*.55,center.z+size*1.15);const controls=new OrbitControls(camera,renderer.domElement);controls.target.copy(center);controls.enableDamping=true;controls.maxPolarAngle=Math.PI/2;const observer=new ResizeObserver(()=>{renderer.setSize(host.clientWidth,host.clientHeight);camera.aspect=host.clientWidth/host.clientHeight;camera.updateProjectionMatrix()});observer.observe(host);renderer.setAnimationLoop(()=>{controls.update();renderer.render(scene,camera)});return()=>{renderer.setAnimationLoop(null);observer.disconnect();controls.dispose();disposeModel(scene);renderer.dispose();renderer.domElement.remove()}},[id]);return <div className="scene" ref={ref}>{error&&<p className="scene-error">WebGL kullanılamıyor. GLB dosyasını indirebilirsiniz.</p>}</div>}
