import { integer, sqliteTable, text, index } from 'drizzle-orm/sqlite-core';
export const users=sqliteTable('users',{id:text('id').primaryKey(),email:text('email').notNull().unique(),name:text('name').notNull(),passwordHash:text('password_hash').notNull(),role:text('role').notNull(),active:integer('active').notNull().default(1),createdAt:text('created_at').notNull()});
export const sessions=sqliteTable('sessions',{tokenHash:text('token_hash').primaryKey(),userId:text('user_id').notNull().references(()=>users.id),expiresAt:integer('expires_at').notNull()});
export const attempts=sqliteTable('auth_attempts',{key:text('key').primaryKey(),count:integer('count').notNull(),resetAt:integer('reset_at').notNull()});
export const settings=sqliteTable('settings',{key:text('key').primaryKey(),value:text('value').notNull()});
export const territories=sqliteTable('territories',{key:text('key').primaryKey(),continent:text('continent').notNull(),country:text('country'),userId:text('user_id').notNull().references(()=>users.id)});
export const regions=sqliteTable('regions',{id:text('id').primaryKey(),country:text('country').notNull(),name:text('name').notNull(),data:text('data').notNull(),revision:integer('revision').notNull().default(1),updatedAt:text('updated_at').notNull(),updatedBy:text('updated_by').notNull().references(()=>users.id)});
export const projects=sqliteTable('projects',{id:text('id').primaryKey(),ownerId:text('owner_id').notNull().references(()=>users.id),salesRepId:text('sales_rep_id').references(()=>users.id),name:text('name').notNull(),country:text('country'),regionId:text('region_id').references(()=>regions.id),data:text('data').notNull(),revision:integer('revision').notNull(),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull(),archived:integer('archived').notNull().default(0)},t=>[index('projects_owner').on(t.ownerId),index('projects_rep').on(t.salesRepId)]);
export const drafts=sqliteTable('drafts',{userId:text('user_id').primaryKey().references(()=>users.id),data:text('data').notNull(),updatedAt:text('updated_at').notNull()});
export const setupDrafts=sqliteTable('setup_drafts',{id:text('id').primaryKey(),userId:text('user_id').notNull().references(()=>users.id),data:text('data').notNull(),updatedAt:text('updated_at').notNull()},t=>[index('setup_drafts_user').on(t.userId)]);
export const customers=sqliteTable('customers',{id:text('id').primaryKey(),ownerId:text('owner_id').notNull().references(()=>users.id),projectId:text('project_id').notNull().unique().references(()=>projects.id),data:text('data').notNull(),updatedAt:text('updated_at').notNull()});
export const quotes=sqliteTable('quotes',{id:text('id').primaryKey(),number:text('number').notNull().unique(),requestId:text('request_id').notNull().unique(),projectId:text('project_id').notNull().references(()=>projects.id),ownerId:text('owner_id').notNull().references(()=>users.id),customerId:text('customer_id').notNull().references(()=>customers.id),currency:text('currency').notNull(),status:text('status').notNull(),snapshot:text('snapshot').notNull(),createdAt:text('created_at').notNull()});
export const events=sqliteTable('events',{id:text('id').primaryKey(),userId:text('user_id').notNull().references(()=>users.id),projectId:text('project_id'),type:text('type').notNull(),data:text('data').notNull(),createdAt:text('created_at').notNull()},t=>[index('events_project').on(t.projectId,t.createdAt)]);
